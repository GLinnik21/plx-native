# TV session 3 (phase 5b) — 2026-09-07, panel OFF

Debug install, `make deploy` md5-verified 15/15 files. Panel off for the whole session per the
owner's standing rule; no playback in the UI tier, so nothing needed muting.

## fps: 30 passed, 0 failed of 30

Every UI-tier scene at its floor/ceiling, including all eleven of the migrated Settings family.
The family's active scenes all read a 60 fps median; the idle ones settle to the keepalive.

| scene | result |
|---|---|
| settings-root / -privacy / -legal / -home | median 60 fps (floor 50) |
| settings-idle | robust_max 1 fps (ceiling 5) |
| consent-crash / consent-product | median 60 fps (floor 50) |
| onboard-sources | median 60 fps (floor 50) |
| modal-ramp | worst robust_max **63.0 ms** (ceiling 75) |
| legal-document | worst robust_max 2.6 ms (ceiling 33) |
| decision-alert | worst robust_max 3.6 ms (ceiling 40) |

Two notes worth carrying:

- **`fps:settings-home` matched samples (n=12).** This phase changed the scene's selection words
  from `route=onboard` to `route=home overlay=onboard`, because the Home-sources editor stopped
  being a route and became a page inside the Settings surface. A word-coupling break shows up as
  "no samples", not as a bad number, and the host cannot prove the coupling — so this run is the
  evidence that it works.
- **`modal-ramp` landed in the FAST mode** (63.0 ms). The manifest records this scene as bimodal
  on main itself — ~65 ms or ~94 ms, same binary, same session, cause not understood. A single
  reading here is therefore not evidence about phase 5b in either direction; it is only evidence
  that the ramp did not get worse than the ceiling that was already held.

## Soft-float differential table: MATCH

    softfloat: n=4096 hash=0x65a8e905a259246d host=0x65a8e905a259246d MATCH

Owed since phase 2. Full account and its limits: `docs/measurements/softfloat-arm-tv-2026-09-07.md`.
