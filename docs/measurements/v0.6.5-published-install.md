# v0.6.5 published-package installation — 2026-09-11

The stable package downloaded from [v0.6.5](https://github.com/GLinnik21/plx-native/releases/tag/v0.6.5) was installed through the TV's `com.webos.appInstallService/dev/install` endpoint. The installer reached `statusValue=30`, `state=installed`, with `update=true`. No local executable was deployed over the installation.

[Release run 34610339395](https://github.com/GLinnik21/plx-native/actions/runs/34610339395) passed all five jobs. Every asset was uploaded by `github-actions[bot]`; the published-download check passed all seven assertions, and the generated audit landed on `release/v0.6` in `0f008876`.

- Package SHA256: `2c11a1e5cc38a2734b5e29779aae52612ddf3ae02555771a052a80469981e54f`, matching the downloaded checksum file.
- Published ELF SHA256: `5b598603373305a8c44fa7f6a90011a47edff5a51b7b6580f03bcde2fad6aa2a`, matching the executable in the installed stable app directory.
- Installed descriptor: `com.beb.plxnative`, version `0.6.5`.
- Fresh startup witness: `install: id=com.beb.plxnative flavour=- runtime=/tmp features=release APPID_env=com.beb.plxnative`.
- Dev TV: LG 49SM9000PLA, `m16p3`, platform release `4.10.2`. The process remained alive and logged `route=profiles` after an ordinary launch.
- No stable remote FIFO or port 8910 listener. The stable runtime had its diagnostic logs and Sentry database; no development triggers were armed.

The app was left at its normal profile picker, audio muted and backlight state unchanged; the TV lock was released. Existing user data was retained. No playback or repair was attempted, so this check proves package installation and startup on the dev set only. The affected Realtek repair-button and experimental non-root procedure limitations in the release note remain.
