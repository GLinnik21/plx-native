> **Update, 11 August 2026 — three corrections and one disclosure.** This note is the archival
> record of what was claimed on 6 August and is left standing below. Later evidence contradicted
> parts of it.
>
> **1. Rotate your Plex token if you ran this version.** The section below describes the event log
> as recording "your server name, address and what you have been watching". That was incomplete: it
> could also contain your Plex access token in full, written whenever an audio track was switched,
> because a transcode URL carries the token in its query string. The same note asks readers to send
> that file in when reporting a problem. Fixed in
> [v0.2.1](https://github.com/GLinnik21/plx-native/releases/tag/v0.2.1), which also scrubs the log
> at its single sink so the mistake cannot recur. If you have ever shared a
> `/tmp/plxnative-events.log` from this version or earlier, treat the token as disclosed and rotate
> it at <https://app.plex.tv/desktop/#!/settings/devices>.
>
> **2. The webOS 5+ prediction was wrong.** Below it says the video-plane replacement "has never run
> on an actual set, so you may get sound and a black picture". Tested since on real webOS 6 and 10
> televisions: the app runs, signs in and browses, but playback does not start at all — it sits on a
> spinner. Not sound with a black picture. v0.2.1 adds an on-screen diagnostics panel so a report
> from that hardware is possible without a shell.
>
> **3. The build is not reproducible.** Below it says "the same commit and toolchain give a
> byte-identical `.ipk`, so you can rebuild and compare". Two builds on **one machine** are
> identical, but the bundled FFmpeg records the directory it was built in, so a rebuild anywhere
> else differs. That is not tampering. Use the sha256 alone.
>
> **4. `sha256sum -c ipk.sha256` does not work for this release.** The checksum asset carries a
> `pkg/` path prefix instead of the bare filename, so it looks for a file that is not there. Hash
> the `.ipk` directly and compare against the block below.
>
> One figure below is also miscast: "4.7 MB to 5.3 MB" matches no consistent unit. The real sizes
> are 4,861,038 bytes for v0.1.0 and 5,519,534 for this one.

---
The app no longer depends on which FFmpeg your television happens to ship, and it starts on a much
wider range of webOS releases than before.

## It installs on more televisions

v0.1.0 could not start at all past webOS 4.x — not "playback failed", but the dynamic loader
refusing to launch the process, because the app named libraries that only exist on 4.x. That is
fixed: it now starts on **every firmware it has been checked against — webOS 4.4.2 through
11.2.0**, i.e. sets from 2018 to the newest in webosbrew's compatibility database — because it
resolves what it needs at run time instead of demanding one exact version.

**webOS 4.x remains the only version tested.** On webOS 5 and newer LG replaced the mechanism that
puts decoded video on the hardware plane; the replacement is implemented here, but it has never run
on an actual set, so you may get sound and a black picture. If you try it, please open an issue and
say what happened — the app records what it chose in `/tmp/plxnative-events.log`, and the first
thirty lines of that settle it either way. Even "the interface works, the video is black" is a
useful report.

## It brings its own FFmpeg

Previously the app used the television's FFmpeg to read your media files. That version changes with
the firmware, and there is no way from outside to tell which parts of it LG compiled in — so on any
set other than the author's, it was a guess.

The package now ships its own build (FFmpeg 9.0, demux and subtitles only — video and audio are
still decoded by your TV's hardware, as they should be). Playback behaves the same on every
television, and a problem you hit is a problem that can be reproduced.

This adds about 0.6 MB to the download (4.7 MB to 5.3 MB).

## Fixes

- **A latent black-screen bug.** The app told SDL it was built against an older version than it
  was. Newer SDL rejects that and quietly hands back nothing, which would have left the interface
  opaque and video playing invisibly underneath it — sound, no picture, and nothing in the log.
- **The diagnostic log was corrupting itself.** Two parts of the app wrote to it with different
  ideas of where the end of the file was, so lines were being overwritten and lost. It also has
  sane permissions now: it records your server name, address and what you have been watching, and
  it used to be world-readable.
- Groundwork for televisions whose interface surface is not 1080p, and a lot of internal
  simplification.

## Installing

Grab the `.ipk` and install it with the [Homebrew
Channel](https://github.com/webosbrew/webos-homebrew-channel) or
[dev-manager-desktop](https://github.com/webosbrew/dev-manager-desktop). You do **not** need a
rooted TV; the app runs in LG's normal sandbox.

Nothing in this distribution chain is code-signed, so the hash is the only thing between you and a
tampered package. It is worth checking:

```
ef5983c8cd523187625b937f36fa0946145244f7cac4e902b499d9553e5d1968  com.beb.plxnative_0.2.0_arm.ipk
```

Builds are reproducible — the same commit and toolchain give a byte-identical `.ipk`, so you can
rebuild and compare with `make RELEASE=1 ipk`.

## Source for the bundled FFmpeg

`ffmpeg-9.0.tar.xz` and `build-ffmpeg.sh` are attached to this release. FFmpeg is LGPL-2.1 and
shipping it makes that our obligation rather than the television's: the tarball is unmodified
upstream, the script carries the complete configure invocation that produced the libraries, and
they are separate `.so` files you are free to replace with your own build. Details in
`THIRD-PARTY-NOTICES.md`, which travels inside the package.

## Still the same scope

Movies and TV shows, from a Plex Media Server on your local network. No music, photos, live TV or
DVR, and no way to enter a remote server address.

---

## Appended 11 August 2026 — the sections this note was missing

The prose above is unchanged. What follows is the material `docs/release-notes/README.md` now
requires of every release, filled in from this release's own artifacts.

### Which televisions this works on

| Firmware | State |
|---|---|
| webOS 4.10 | Verified on a real television: the app runs and playback works. |
| webOS 4.4.2 – 11.2.0 | The app **starts** — checked against 14 real firmware inventories. |
| webOS 5.0 and newer | Runs and browses; **playback does not start** (see correction 2 above). |

### Checking what you downloaded

Nothing anywhere in this distribution chain is signed — there is no code signing in the webosbrew
path at all — so this sha256 is what tells you the file you have is the file that was published
here.

```
ef5983c8cd523187625b937f36fa0946145244f7cac4e902b499d9553e5d1968  com.beb.plxnative_0.2.0_arm.ipk
```

Check it with `shasum -a 256 com.beb.plxnative_0.2.0_arm.ipk` on macOS or Linux, or
`certutil -hashfile com.beb.plxnative_0.2.0_arm.ipk SHA256` on Windows. The `ipk.sha256` asset
cannot be used with `-c` for this release — see correction 4.

If the Homebrew Channel installs this for you from its catalogue, it fetches this release's
`com.beb.plxnative.manifest.json`, hashes the download on the television and refuses to install a
package that does not match — you have nothing to do. Pointing the Channel at a bare `.ipk`
yourself skips that check, so check it yourself.

Built by GitHub Actions from commit `cf5334a`. The assets attached here are that run's artifacts,
unmodified — nothing was rebuilt or re-uploaded by hand.

### Source for the bundled FFmpeg

This package contains three FFmpeg shared libraries — `libavformat-plx.so.63`,
`libavcodec-plx.so.63` and `libavutil-plx.so.61` — built from **FFmpeg 9.0**, unmodified, and
licensed **LGPL-2.1-or-later**. Demuxers, parsers, bitstream filters and subtitle decoders only:
video and audio are decoded by the television's own hardware.

The complete corresponding source is attached to this release:

- `ffmpeg-9.0.tar.xz` — the pristine upstream tarball, sha256
  `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52`, from
  <https://ffmpeg.org/releases/>. No patches are applied.
- `build-ffmpeg.sh` — the complete configure invocation that produced the libraries. Built with
  `--disable-everything` plus an explicit component list, and **without** `--enable-gpl`,
  `--enable-version3` or `--enable-nonfree`, so no GPL or non-free component is present.

They are ordinary shared libraries, `dlopen`ed by absolute path from the app's own directory under
exactly those names — so they can neither shadow nor be shadowed by the television's own FFmpeg,
and a build of your own with the same names replaces ours. Full text in `THIRD-PARTY-NOTICES.md`
and `licenses/LGPL-2.1.txt`, both inside the package.

### Package facts

| | |
|---|---|
| Download | 5,519,534 bytes |
| Installed | 9,115 KiB |
| Needs root | No |
| Bundled shared libraries | 3 (FFmpeg 9.0, LGPL-2.1-or-later) |

### Known not to work

- webOS 5.0 and newer: playback does not start.
- Codec capability is asserted rather than probed.
- The event log discloses more than this note said — see correction 1.

