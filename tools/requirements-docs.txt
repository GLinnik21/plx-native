# Pinned dependency for tools/render-doc-page.py. Bump deliberately, not with a floor:
# a floated version can change table/heading rendering under the site with no diff to review.
markdown-it-py==3.0.0
