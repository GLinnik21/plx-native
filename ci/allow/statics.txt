# count: 9
rust-modules/src/ui/icons.rs	CACHE: rasterised icon masks keyed by (name, size) — a GL texture cache, never read as state
rust-modules/src/ui/text_view.rs	WRAP_CACHE: wrapped-line layout memo keyed by (text, width, size) — recomputed from the same inputs
rust-modules/src/ui/testpat.rs	CURRENT: the dev synthetic ground pattern (plxnative-testpat / pat: token), draw-only
rust-modules/src/ui/anim.rs	ENTRIES: per-key animation clocks for draw-time ramps, render half only
rust-modules/src/ui/popover.rs	CACHE: THE single full-viewport FrameCache (8.29 MB) a covered host is drawn from as a quad
rust-modules/src/ui/popover.rs	HELD: whether that FrameCache is held or being recaptured — the cache's own bookkeeping
rust-modules/src/ui/widgets.rs	SOURCE: profile_chip's own re-derivation of the profile DTO, keyed by session generation — the account-menu scrim's Scrim::lift is a bare fn(f32, Option<GlassFace>) with no &Bridge to borrow, so this path can never reach app::chrome::ChromeSnapshot's real one; recomputed deterministically from the session, never read as state
rust-modules/src/ui/widgets.rs	CHIP: the profile chip's CString/width memo, keyed by (generation, thumb, initial, label) — shared by Bridge::draw_chrome's real call and profile_chip's bare-fn re-derivation above, both of which draw the SAME chip for the SAME generation; recomputed deterministically, never read as state
rust-modules/src/ui/widgets.rs	TAB_CACHE: the tab row's CString/width memo, keyed by browse::tabs_gen() — reached from both Bridge's captured TabLabels calls and, via with_legacy_tab_labels, profile_chip's bare-fn Scrim::lift (bar_glass_wanted); recomputed deterministically from the generation, never read as state
