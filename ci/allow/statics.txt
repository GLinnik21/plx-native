# count: 7
rust-modules/src/ui/icons.rs	CACHE: rasterised icon masks keyed by (name, size) — a GL texture cache, never read as state
rust-modules/src/ui/text_view.rs	WRAP_CACHE: wrapped-line layout memo keyed by (text, width, size) — recomputed from the same inputs
rust-modules/src/ui/testpat.rs	CURRENT: the dev synthetic ground pattern (plxnative-testpat / pat: token), draw-only
rust-modules/src/ui/anim.rs	ENTRIES: per-key animation clocks for draw-time ramps, render half only
rust-modules/src/ui/popover.rs	CACHE: THE single full-viewport FrameCache (8.29 MB) a covered host is drawn from as a quad
rust-modules/src/ui/popover.rs	HELD: whether that FrameCache is held or being recaptured — the cache's own bookkeeping
rust-modules/src/ui/widgets.rs	TAB_CACHE: tab-row CString/width memo keyed by the full captured (generation, labels) vocabulary; both normal chrome and its scrim lift pass the same Bridge-owned labels, and the cache is never read as state
