//! Owned Library screen (phase 8). The implementation is split by responsibility so stable
//! identity, geometry, focus parts and deferred store transactions can be tested independently.

mod identity;
mod layout;
mod parts;
mod transactions;

